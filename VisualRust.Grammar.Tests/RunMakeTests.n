using Nemerle;
using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using System.Collections.Generic;
using System.Linq;
using System.IO;

using MSTestHacks;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace VisualRust.Grammar.Tests
{
    [TestClass]
    public class RunMakeTests : TestBase
    {
        static allFiles : IEnumerable[string]
        {
            get { Directory.EnumerateFiles(@"..\..\..\rust\src\test\run-make", "*.rs", SearchOption.AllDirectories) }
        }

        [TestMethod]
        [DataSource("VisualRust.Grammar.Tests.RunMakeTests.allFiles")]
        public TestRunMake() : void
        {
            def path = TestContext.GetRuntimeDataSourceObject.[string]();
            ParserVerifier.VerifyPositiveFile(path);
        }        
    }
}
