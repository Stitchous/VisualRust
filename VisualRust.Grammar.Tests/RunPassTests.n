using Nemerle;
using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using System.Collections.Generic;
using System.Linq;
using System.IO;

using MSTestHacks;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace VisualRust.Grammar.Tests
{
    [TestClass]
    public class RunPassTests : TestBase
    {
        [TestMethod]
        [DataSource("VisualRust.Grammar.Tests.RunPassTests.allFiles")]
        public TestRunPass() : void
        {
            def file = Parser.ParseFileWithExtendedStack(TestContextPath);               
            Assert.IsTrue(file.ParseResult.IsSuccess, file.GetMessage());
        }
        
        #region DataSource
        
        TestContextPath : string
        {
            get { TestContext.GetRuntimeDataSourceObject.[string]() }
        }        
                    
        static allFiles : IEnumerable[string]
        {
            get { Directory.EnumerateFiles(@"..\..\..\rust\src\test\run-pass", "*.rs", SearchOption.AllDirectories) }
        }
        
        #endregion
    }
}
